{
  "name": "path-game",
  "version": "1.0.0",
  "description": "",
  "distDir": "./dist/",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "browser-sync start --cors --server './' --files 'index.html' --no-open --no-inject-changes",
    "dev": "concurrently --kill-others \"parcel watch src/*.js\" \"npm run serve\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.27.5",
    "concurrently": "^6.3.0",
    "parcel": "^2.4.0",
    "phaser": "^3.55.2"
  }
}
